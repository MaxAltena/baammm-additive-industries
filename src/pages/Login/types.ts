export interface LoginProps {
	/**
	 * Show image to the side of the login form
	 */
	withImage?: boolean;
	/**
	 * Show options below form for remembering and forgotten password
	 */
	withOptions?: boolean;
}
